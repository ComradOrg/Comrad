##
# Run THE OPERATOR
##


SCRIPTPATH="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

path=`realpath "$SCRIPTPATH/../komrade/backend/switchboard.py"`
pathvenv=`realpath "$SCRIPTPATH/../venv/bin/activate"`

source $pathvenv
date=$(date '+%Y-%m-%d')
pathlog=`realpath "$SCRIPTPATH/../log/$date.op.log"`

echo $pathlog

cmd="python $path 8080 $* >> $pathlog 2>&1"

# do forever
while true; do $cmd && break; done


#if command -v deactivate &> /dev/null
#then
#    deactivate
#fi
